# RoomNoteApp

## What i learned:

-Setting up Room Database in Kotlin.

-Creating @Dao interfaces for CRUD operations.

-Managing data through ViewModel to ensure lifecycle awareness.

-Persisting note data even after the app is closed or restarted.

-Using @Insert, @Update, @Delete, and custom @Query annotations.

-Handling database migrations for schema changes.
